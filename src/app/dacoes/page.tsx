import DacoesHomePageClient from '@/components/dacoes/home-page-client';

export const metadata = {
  title: 'Dações | Grupo Plaenge',
  description: 'Apartamentos de alto padrão adquiridos por dação em pagamento. Disponibilidade imediata. Porto Alegre / RS.',
};

export default function DacoesPage() {
  return <DacoesHomePageClient />;
}
